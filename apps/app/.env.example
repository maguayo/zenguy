# Copy to .env.local for local development (ignored by git).
# The local Wrangler API runs on http://localhost:8787; use your Mac's LAN IP
# instead of 127.0.0.1 when running on a physical iPhone.
EXPO_PUBLIC_API_ORIGIN=http://127.0.0.1:8787
