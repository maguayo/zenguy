{
  "name": "@zenguy/website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4400",
    "generate:favicons": "node scripts/generate-favicons.mjs",
    "build": "astro build",
    "preview": "astro preview --port 4400",
    "typecheck": "astro build",
    "test": "node --test test/*.test.mjs",
    "deploy": "echo 'Deploys automatically via Cloudflare Pages on git push (project: zenguy)'"
  },
  "dependencies": { "astro": "^7.3.1" },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "sharp": "0.35.2",
    "tailwindcss": "^4.3.3",
    "wrangler": "^4.123.0"
  }
}
