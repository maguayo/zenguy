browser-use[core]==0.13.8
httpx==0.28.1
# Security floors for vulnerable transitive dependencies. Keep these explicit so
# a browser-use resolver change cannot silently reintroduce the advisories.
click>=8.4.2
mcp>=1.28.1,<2
pypdf>=6.15.0
pip>=26.2
